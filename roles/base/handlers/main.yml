---
# roles/base/handlers/main.yml
# Los handlers se ejecutan UNA sola vez al final del play,
# solo si fueron notificados por alguna tarea

- name: save config
  cisco.ios.ios_config:
    save_when: modified
