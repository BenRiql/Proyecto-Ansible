---
# Colecciones necesarias para el proyecto
# Instalar con: ansible-galaxy collection install -r requirements.yml

collections:
  - name: cisco.ios
    version: "4.6.1"
  - name: ansible.netcommon
    version: "5.3.0"
  - name: ansible.utils
    version: "3.1.0"
